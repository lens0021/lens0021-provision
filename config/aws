[profile port-aws-infra]
region = ap-northeast-2
credential_process = bash -c "saml2aws login --skip-prompt --username=leslie --password=$PORT_KEYCLOAK_PW --credential-process -a port-aws-infra --mfa-token=$(oathtool --base32 $PORT_KEYCLOAK_OTP --totp)"

[profile port-aws-dev]
region = ap-northeast-2
credential_process = bash -c "saml2aws login --skip-prompt --username=leslie --password=$PORT_KEYCLOAK_PW --credential-process -a port-aws-dev --mfa-token=$(oathtool --base32 $PORT_KEYCLOAK_OTP --totp)"

[profile port-aws-stg]
region = ap-northeast-2
credential_process = bash -c "saml2aws login --skip-prompt --username=leslie --password=$PORT_KEYCLOAK_PW --credential-process -a port-aws-stg --mfa-token=$(oathtool --base32 $PORT_KEYCLOAK_OTP --totp)"

[profile port-aws-prod]
region = ap-northeast-2
credential_process = bash -c "saml2aws login --skip-prompt --username=leslie --password=$PORT_KEYCLOAK_PW --credential-process -a port-aws-prod --mfa-token=$(oathtool --base32 $PORT_KEYCLOAK_OTP --totp)"

[profile port-intl-dev]
region = ap-southeast-1
credential_process = bash -c "saml2aws login --skip-prompt --username=leslie --password=$PORT_INTL_KEYCLOAK_PW --credential-process -a port-intl-dev --mfa-token=$(oathtool --base32 $PORT_INTL_KEYCLOAK_OTP --totp)"

[profile port-intl-stg]
region = ap-southeast-1
credential_process = bash -c "saml2aws login --skip-prompt --username=leslie --password=$PORT_INTL_KEYCLOAK_PW --credential-process -a port-intl-stg --mfa-token=$(oathtool --base32 $PORT_INTL_KEYCLOAK_OTP --totp)"

[profile port-intl-prod]
region = ap-southeast-1
credential_process = bash -c "saml2aws login --skip-prompt --username=leslie --password=$PORT_INTL_KEYCLOAK_PW --credential-process -a port-intl-prod --mfa-token=$(oathtool --base32 $PORT_INTL_KEYCLOAK_OTP --totp)"

[profile fw]
sso_session = 2
sso_account_id = 302617221463
sso_role_name = AdministratorAccess
region = ap-northeast-1
[sso-session 2]
sso_start_url = https://femiwiki.awsapps.com/start/#
sso_region = ap-northeast-2
sso_registration_scopes = sso:account:access
